# PdfRedact
PdfRedact is a free, open-source .NET tool for masking sensitive text in PDFs. It detects text using configurable rules, applies visual mask overlays with PDFsharp, and optionally flattens pages into image-only PDFs to prevent underlying text extraction.
